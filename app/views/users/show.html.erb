<div class='profile-head'>
	<h1>Hi <%= current_user.first_name %> !</h1>
	<%= button_to 'Main Page', root_path, method: :get, class: "main-page-button" %>
</div>

<hr>

email: <%= current_user.email %>

<h4>Your reservation</h4>
<h5>Details</h5>
<% @reservation.each do |show_reservation| %>
	States:<%= show_reservation.listing.state %>
	<br>
	Address:<%= show_reservation.listing.address %>
	<br>
	Postcodes:<%= show_reservation.listing.postcode %>
	<br>
	Date: <%= show_reservation.check_in_date %>
	<hr>
<% end %>


<%= link_to "Make Payment", braintree_new_path %>